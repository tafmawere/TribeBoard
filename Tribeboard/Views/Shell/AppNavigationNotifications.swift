import Foundation

extension Notification.Name {
    /// Switch to Family tab so the user can add or enable drivers.
    static let tribeboardOpenFamilyForDriverSetup = Notification.Name("tribeboard.openFamilyForDriverSetup")
}
